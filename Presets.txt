# Please add any preset with a basic description in one line, 
# and ffmpeg parameters on the next lines. NOTE: Not all presets
# could be supported by FFmpeg Batch AV Converter, or only using
# a wizard (those that reuse input files on parameters field require
# the use of variables like %1 or %fn).
# Example:

Description: Video: Convert video to rgb uncompressed avi (huge file).
Pre-input:
Parameters: -c:v rawvideo -pix_fmt rgb24
Format: avi

Description: Generate keyframes report to txt file.
Pre-input: -skip_frame nokey
Parameters: -vstats_file C:\Datos\stats.txt -vf "select='eq(pict_type,PICT_TYPE_I)'" -vsync vfr -frame_pts true -f null -
Format: nul

